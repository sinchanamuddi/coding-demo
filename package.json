{
  "name": "yatraai-india-travel-planner",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "dev:api": "node server/server.js",
    "build": "vite build",
    "preview": "vite preview --host",
    "check": "node --check app.js && node --check server/server.js"
  },
  "devDependencies": {
    "vite": "^7.1.0"
  }
}
